services:
  worker:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.docker
    volumes:
      - worker-storage:/app/storage
    restart: "no"

volumes:
  worker-storage:
